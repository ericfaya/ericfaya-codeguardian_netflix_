name: process-pull-request

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  process-pull-request:
    runs-on: ubuntu-latest

    env:
      GROQAPIKEY: ${{ secrets.GROQAPIKEY }}
      GROQMODEL: ${{ secrets.GROQMODEL }}
      GITHUB_TOKEN: ${{ secrets.TOKEN_GITHUB }}
      GROQAPIKEYSUMMARY: ${{ secrets.GROQAPIKEYSUMMARY }}

    steps:
      - name: Checkout code with full history
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Fetch base branch
        run: git fetch origin ${{ github.event.pull_request.base.ref }}

      - name: Get PR SHAs
        run: |
          echo "PR_SHA=${{ github.event.pull_request.head.sha }}" >> $GITHUB_ENV
          echo "PR_SHA_BASE=${{ github.event.pull_request.base.sha }}" >> $GITHUB_ENV

      - name: Get files changed in latest commit of PR
        id: get_changed_files
        run: |
          git diff --name-only ${{ env.PR_SHA }}^ ${{ env.PR_SHA }} > changed_files.txt
          echo "🔍 Archivos cambiados en el último commit:"
          cat changed_files.txt

      - name: Get Pull Request Info
        run: |
          echo "PR_NUMBER=${{ github.event.pull_request.number }}" >> $GITHUB_ENV
          echo "REPO_OWNER=${{ github.repository_owner }}" >> $GITHUB_ENV

      - name: Run CodeGuardian JAR
        run: |
          java -jar codeguardian-0.0.1-SNAPSHOT.jar  \
            ${{ env.GROQAPIKEY }} \
            ${{ env.GROQMODEL }} \
            ${{ env.GITHUB_TOKEN }} \
            ${{ env.REPO_OWNER }} \
            ericfaya-codeguardian_netflix_ \
            ${{ env.PR_NUMBER }} \
            changed_files.txt

      - name: Notificar en Teams (comentarios PR)
        if: success()
        run: |
          PR_NUMBER=${{ github.event.pull_request.number }}
          PR_TITLE="${{ github.event.pull_request.title }}"
          PR_AUTHOR="${{ github.event.pull_request.user.login }}"
          PR_URL="${{ github.event.pull_request.html_url }}"
          COMMIT_MSG=$(git log --no-merges -1 --pretty=%s)
          COMMIT_SHA=$(git log --no-merges -1 --pretty=%h)
          git fetch origin ${{ github.event.pull_request.base.ref }}

          DIFF=$(git diff origin/${{ github.event.pull_request.base.ref }}...${{ github.event.pull_request.head.sha }} --unified=0 | head -c 4000)

          echo "📄 Diff generado para el resumen:"
          echo "$DIFF"

          # Guardamos JSON del prompt en archivo temporal
          echo '{
            "model": "compound-beta",
            "messages": [
              {
                "role": "system",
                "content": "Eres un revisor técnico que resume Pull Requests para incluir un resumen breve, claro y profesional."
              },
              {
                "role": "user",
                "content": "Resume esta PR para incluirlo como comentario automático:\n\n'"${DIFF}"'"
              }
            ],
            "temperature": 0.5
          }' > body.json

          # Llamada a la API de Groq
          curl -s https://api.groq.com/openai/v1/chat/completions \
            -H "Authorization: Bearer $GROQAPIKEYSUMMARY" \
            -H "Content-Type: application/json" \
            -d @body.json > groq_output.json

          SUMMARY=$(jq -r '.choices[0].message.content' groq_output.json)

          if [ -z "$SUMMARY" ] || [ "$SUMMARY" = "null" ]; then
            SUMMARY="(No se pudo generar el resumen)"
          fi

          FINAL_MESSAGE="🛡️ CodeGuardian ha analizado la PR #${PR_NUMBER}
            🔀 ${PR_TITLE}
            👤 Autor: ${PR_AUTHOR}
            🧾 Commit: ${COMMIT_SHA} - ${COMMIT_MSG}
            🔗 Ver Pull Request: ${PR_URL}
            💡 Resumen:
            ${SUMMARY}"

          # Escape JSON para Teams
          ESCAPED_MESSAGE=$(printf '%s' "$FINAL_MESSAGE" | jq -Rs .)

          curl -H "Content-Type: application/json" \
               -d "{\"text\": ${ESCAPED_MESSAGE}}" \
               "${{ secrets.TEAMS_WEBHOOK_URL }}"
